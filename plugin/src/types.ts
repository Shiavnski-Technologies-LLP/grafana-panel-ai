export interface SimpleOptions {
  initialPrompt?: string; // Stores the initial chatbot prompt (configured in panel options)
  userInput?: any; // Stores the latest user input from the panel options input box
}